/*
    COMPOSE VARIABLES (VECTOR/ARRAY):

        Syntax:
			<array_name>[<field1>, <field2>, <field3>, ..., <fieldN>]

		Atributtion (Specific Position)
			<array_name>[<field>] = <value>

		Atributtion (Last Position)
			<array_name>.push(<value>)

		Array Size
			<array_name>.length

		Array order (Increasing)
			<array_name>.sort()

		Array display

			for(let <index_name> = <initial_position>; <index_name> < <array_name>.length>; <index_name><step>){
				console.log(<array_name>[<index_name>])
			}

			Simplifying:

			for(let <index_name> in <array_name>){
				console.log(<array_name>[<index_name>])
			}

		Array Display (POSITION)
			<array_name>.indexOf(<value>)


*/