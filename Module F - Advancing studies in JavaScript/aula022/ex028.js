/*
    OBJECTS:

        Syntax:

			let <object_name> = {<f1_index_name>:<f1_value>, <f2_index_name>:<f2_value>, ..., <fn_index_name>:<fn_value>}

			field values can be strings, integer/float/double numbers, boolean values
			field values can also store functions inside
*/